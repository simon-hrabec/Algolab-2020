#include <iostream>

void solve(const int xyz_count) {

}

int main() {
  std::ios_base::sync_with_stdio(false);
  while(true) {
    int xyz_count;
    std::cin >> xyz_count;
    if (xyz_count == 0) {
      break;
    }
    solve();
  }
  return 0;
}
